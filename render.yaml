# Render Blueprint - Backend Service
services:
  - type: web
    name: jntuh-academic-insights-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    region: oregon
    plan: free
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
